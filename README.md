# X-Splatter

Feed-Forward Metric 4D Gaussian Splatting for Surround-View Driving Scenes.

The project page lives in [`docs/`](docs/) and is served by GitHub Pages
(Settings → Pages → Deploy from a branch → `main` / `docs`).

- Authors: anonymous during review. Edit the `AUTHORS` block in `docs/index.html`.
- Videos: add H.264 `.mp4` files to `docs/static/videos/` and list them in
  `VIDEOS` at the top of `docs/static/js/main.js`.
- Preview locally: `cd docs && python -m http.server 8000`.
